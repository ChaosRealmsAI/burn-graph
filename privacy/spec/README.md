# burn-graph Product Private

This directory owns the formal product, User Paths, BDD, architecture,
milestone, slice, and generated Evidence contracts for burn-graph.

The public code never imports these files. Runtime prompts, results, SQLite
state, raw browser traces, and unredacted logs stay in ignored runtime paths.
