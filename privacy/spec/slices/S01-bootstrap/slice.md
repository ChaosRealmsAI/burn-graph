# S01 Bootstrap

User outcome: an AI initializes a project, applies a valid graph, starts it,
claims a Task, and reports completion using stable JSON.

Includes GraphSpec validation, SQLite bootstrap, Start/Task/End transitions,
CLI envelopes, and an initial Mermaid projection. Excludes branching, loops,
Viewer, and release packaging.

Done When UP01 can execute its first Task through the real CLI.
