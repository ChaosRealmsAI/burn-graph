# S03 Live Mermaid viewer

User outcome: a human opens a local page and sees every graph and node status
update live from the same facts the AI changes.

Depends on S01. Covers UP03 and S02 BDD. Includes design system, Product
Preview, read-only HTTP/SSE, Mermaid, responsive and reconnect states.

Done When browser E2E proves visible parity with the CLI snapshot.
