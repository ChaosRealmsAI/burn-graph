# burn-graph Product Preview

Revision `v1` renders deterministic UP02 and UP03 fixtures through the public
design-system Regions. It covers multi-graph overview, parallel Running nodes,
Done/Blocked states, graph detail, Decision routes, long prompt content, empty
state, disconnected state, and narrow viewport behavior.

The preview has no backend and cannot mutate graph state.
